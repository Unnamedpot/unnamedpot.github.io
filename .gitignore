# Jekyll build output
_site/

# Jekyll cache
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Ruby / Bundler (only relevant if building locally)
# Gemfile.lock is committed on purpose: this is an application (a site),
# not a reusable gem, so pinning exact gem versions keeps builds reproducible.
vendor/
.bundle/

# Design exploration artifacts (kept locally, not pushed)
design-demos/
direction-approved.md

# Tool / editor local config
.vscode/
.idea/

# OS junk files
.DS_Store
Thumbs.db